[pytest]
markers =
    slow: uses the scheduler's real clock and takes seconds rather than milliseconds.
        Run by default, including in CI: the fast structural tests around it check
        configuration, while this one checks that a reminder actually fires.
