---
title: Releases
description: Immutable release records — none exist yet for any LensWord product.
---

# Releases

**No release has been published for any LensWord product.** Confirmed via `git tag -l` and `gh release list` — both empty. This page will list an immutable record per release (version, tag, commit SHA, included changes, checksums, and verification evidence) once one exists — see [Release process](/reference/trust/release-process) for how a release will be cut and promoted from `Unreleased` to `Released`.

See each product's changelog for what's changed since the project started, all of it still `unreleased`:
