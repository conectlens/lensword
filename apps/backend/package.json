{
  "name": "lensword-backend-cf-worker",
  "private": true,
  "version": "0.1.0",
  "description": "Cloudflare Worker glue that fronts the backend Container — see wrangler.toml. The backend itself is Python (requirements.txt); this is deploy tooling only.",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "@cloudflare/containers": "^0.3.7",
    "@cloudflare/workers-types": "^5.20260804.1",
    "typescript": "^5.7.0",
    "wrangler": "^4.120.0"
  }
}
