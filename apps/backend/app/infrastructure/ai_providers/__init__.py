"""Concrete AIProvider adapters (issue #315).

Import from `app.infrastructure.ai` instead of this package directly —
that module re-exports everything here and is the stable path every existing
caller (`app/api/deps.py`, the test suite) already uses.
"""
